@model IEnumerable<gorm_Quiz2.Models.WardDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WardName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.PFirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WardName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.PFirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WardId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WardId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WardId })
        </td>
    </tr>
}

</table>
